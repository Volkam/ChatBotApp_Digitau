<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/textoCinzaBold</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BotaoArredondado" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@drawable/botao_arredondado_laranja</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:drawableRight">@drawable/ic_seta_direita</item>


    </style>

</resources>
